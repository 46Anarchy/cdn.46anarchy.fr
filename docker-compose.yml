version: '3.9'
services:
  web:
    build: .
    ports:
      - 127.0.0.1:8080:8080
    env_file: .env
    environment:
      - STORAGE_ROOT=/app/files
    restart: unless-stopped
    volumes:
      - ./files:/app/files
