# Copy this file to .env and set a real password before running.
ADMIN_PASSWORD=replace-me

# Optional base URL for generated manifest and file URLs.
# If set, manifest.json will use this value instead of the request host.
MANIFEST_BASE_URL=https://cdn.example.com
